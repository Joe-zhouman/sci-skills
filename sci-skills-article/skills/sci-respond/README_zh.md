# sci-respond

修回阶段的 Response-to-Reviewers 信。不是"填个致谢模板"的工具——是"在诚实的底线之内，把这篇稿子送过线"的工具。

## 为什么会有这个 skill

我有一篇稿子，被拒过，大修过，小修过，最后中了——这一路下来我才明白，写回复信跟写论文完全是两件事。

论文是写给**想理解你**的读者看的。回复信是写给**已经在怀疑你**的审稿人看的——他花了几小时挑毛病，现在盯着看你到底是真改了还是在打太极。每句话都是被对着读的。一句"我们已经处理了这个问题"如果不附上**到底怎么处理的**，审稿人立刻翻回手稿去查——然后他就烦了。

还有一件事，没哪本写作指南会写进去。审稿人要的有些东西，你真的做不到——设备、时间、样品、钱，都不在那。诚实的回答是"我们做不到，因为 XX"。但同样的诚实，说错了，读起来像"我们做的工作很弱"；说对了，同一件事读起来像"这是方法的已知边界，已经在证据支撑的范围内界定清楚了"。**事实一样，结局不一样。** 这中间的缝——事实本身和事实落到对方眼里的样子之间的缝——就是写回复信的全部手艺。很多 skill 避谈这一层，因为它听起来像不诚实。我需要有人把它说破。

所以这个 skill 存在。它做三件事：

1. **每条回复都自包含。** 审稿人不用翻回手稿。每条回复自带证据——改后的原句（`redline` 标出来）、紧挨着正文的 `Response Figure`、或者数据加精确位置。"详见第 X 节"而不说清怎么改的 = 失败的回复。
2. **起草前先停一下。** skill 把每条审稿意见拆成稳定的 `issue ledger`，挖出底层的关注点（不是字面要求），然后停下来。对每条要紧的意见，列出 2–4 个策略加权衡。你来选。**在你拍板之前，一个字都不起草**——因为"防守还是退守、`claim` 缩不缩、补实验还是引现有证据"这种决定，只有你能做。
3. **框定是技艺，在诚实底线之上。** 不编数据、不假引、不"做了没做的实验"——底线是硬的。但底线之上：重新框定范围、把局限讲小、把弱点转到 `SI`、利用审稿人对你有利的误读。这些是合法的、本来就有的、而且 skill 直接叫出它们的名字（A–G 七类），不假装它们不存在。**目标是中稿；诚实是它站的地板，不是它鞠躬的天花板。**

## 这是给谁用的

不是给处境优越的作者——好学校、好数据、导师把路指好了。对那种作者，"诚实呈现"就是最优策略，他们的工作够硬，诚实本身就能把它送到。他们的 skill 可以把"诚实"当最高原则。

这个 skill 是给处境不优越的作者——学校一般、资源一般、很多工作只能做到这个水平，因为条件就那样。这不是态度问题，是现实。在这个现实下，一个诚实地呈现的"一般"稿件，会被用强工作的标准去衡量，然后死掉。所以这里每一条手艺，都是为了让"一般"的工作在诚实底线之内，争取它该有的中稿机会。黄药师的选择，不是全真教——处境之分，不是道德之分。（家族 `README` 的"这套 skill 服务于谁"那段有更长的版本。）

## 怎么跑

每轮修回四个阶段。细节在 `SKILL.md` + `references/`。

1. **`Intake`** —— `scripts/scan_neighbor.py` 感知 `grounding`（审稿意见、手稿 `tex`、`sci-write` 的写作笔记、`sci-draw` 的图 `report`、已有的 `issue-ledger`），报告哪些在、哪些缺。读它的输出，再去读单个文件。
2. **拆意见 + `checkpoint`** —— 每条审稿意见拿一个稳定 ID（`R1-Q03`）、一个底层关注点、一个建议策略、一个 `safe-claim-boundary`。skill 把 `ledger` 摆出来，**停**。你锁策略。**没锁之前不起草。**
3. **起草** —— 直接写 `response.tex`（`tex`，不是 `Word`——精确排版；你硬要 `Word` 就 `pandoc` 转，转不全的格式你自己补）。封面（三个双盲安全的字段）、按 `reviewer` 逐条、`substance-first` 顺序（事实→证据→立场→落地→可选的致谢放最后）。`typo`/澄清类用 `inline redline`（`\added`/`\deleted`）；`Response Figure` 非浮动（`as-is`，不飘）。
4. **自检 + 编译** —— `scripts/check_response.py` 跑确定性检查（comment/response 配对、残留占位、裸 `\textcolor`、致谢计数、`banned qualifiers`、`float` 说明符、`cover` 字段）。然后是语义检查（`coverage audit`、`integrity firewall`、独立审稿人通读）。当 `session` 编译成 `PDF`。

## `phrasebank` 飞轮

`references/phrasebank.md` 是个活文件。写回复信最难的那部分——**框定话术**（怎么重新框定范围、把局限讲小、礼貌地拒绝一个实验）——在公开渠道是凑不到的。我扫了一圈开源资源（`GitHub` 的 `response-template` 仓库、写作指南），找到的只有空 `LaTeX` 骨架和诚实话术；框定这一层大家集体避谈。所以这个库靠两个真实来源长大：

1. **你自己公开发表的回复信** —— 每封丢到 `assets/samples/<作者>-<年份>-<期刊>/`（`PDF` + `text` 版 `.md`）。`scripts/extract_phrases.py` 帮你扫，抽出框定话术（整句 + 出处 + A–G 猜测），直接是 `Inbox` 格式。
2. **你在帖子 / 同事的信 / 公开 `review` 里看到的好措辞** —— 原样贴进 `Inbox`，回头再分。

每中一封，库就厚一点，下一封就省力一点，下一封又是一份新素材。左脚踩右脚。

`samples/` 目录（`assets/samples/INDEX.md`）同时是**展示橱窗**——每条带论文的 `DOI`，谁都能点进去核实这封信真中过稿。是证据，不是嘴说。

## 这个 skill 不做什么

- **按锁定的决定改手稿 `tex`** → 那是 `sci-revise`（外科手术式改；本 skill 只写 `revision_kind` 字段去驱动它）。
- **润色手稿文字** → `sci-polish`。
- **`cover letter`** → `sci-submit`。
- **画数据图** → `sci-draw`。

## 状态

设计完成（`docs/design-note.md` 记录了每个决定及其理由，包括很多 skill 避谈的框定层）。骨架 + 脚本 + 测试 + 一个真实样本（`Zhou-2025-commeng`——`Communications Engineering`，`DOI` 10.1038/s44172-025-00508-0）。`phrasebank` 现在还薄——这是设计使然，它跟着用越多越厚。

## 反馈

如果你也在不那么理想的条件下写过回复信，碰到过这个 skill 没覆盖到的套路，告诉我。框定手法和 `phrasebank` 都是从真实轮次里长出来的，不是猜出来的。
